<?php

namespace App\Http\Controllers;
use Illuminate\Http\Request;

/**
 * userController
 * * Este controlador fue creado para gestionar la lógica relacionada con los usuarios.
 * Actualmente se encuentra en su estado base (vacío) generado por el comando:
 * 'php artisan make:controller userController'
 * * Este archivo sirve como estructura para futuras implementaciones, como el manejo
 * de perfiles, autenticación o roles de usuario fuera de la lógica de películas.
 */
class userController extends Controller
{
    // En este espacio se definirían métodos como index(), create() o store() 
    // específicos para la entidad de Usuarios en caso de no utilizar Laravel Breeze.
}
